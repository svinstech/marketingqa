HOW TO COMPILE TYPESCIPT FILES

- Navigate to the root directory and run this command: tsc
- This will compile all .ts files that exist anywhere in the ./cypress directory.
- The resulting .js files will be placed in the newly created /dist directory.